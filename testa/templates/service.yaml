apiVersion: v1
kind: Service
metadata:
  name: {{ include "testa.fullname" . }}
  labels:
    {{- include "testa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testa.selectorLabels" . | nindent 4 }}
